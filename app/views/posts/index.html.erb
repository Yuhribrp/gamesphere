  <h1><%= @community.title %></h1>

<div class="main-page">

  <div class="sidebar">
    <ul>
      <li><i class="fas fa-headset"></i>players</li>
      <li><i class="fas fa-headset"></i>communities</li>
      <li><i class="fas fa-headset"></i>matches</li>
    </ul>
  </div>

  <div class="post-msg">

  <% @posts.each do |post| %>
    <% if post.photo %>
      <%= image_tag(post.photo) %>
      <p><%= post.content %></p>
    <% elsif post.video %>
      <%= video_tag(post.video) %>
      <p><%= post.content %></p>
    <% else %>
      <p><%= post.content %></p>
    <% end %>
  <hr>
  <% end %>

    <img src="https://kitt.lewagon.com/placeholder/users/ssaunier" class="avatar" alt="avatar">
    <h2>Good morning, people! Guided link to my game play.. <span>12pm</span></h2>
    <p>Mario é o melhor jogo the todos os tempos!!!! 😍</p>

    <img src="https://kitt.lewagon.com/placeholder/users/ssaunier" class="avatar" alt="avatar">
    <h2>Good morning, people! Guided link to my game play.. <span>12pm</span></h2>
    <p>Mario é o melhor jogo the todos os tempos!!!! 😍</p>
  </div>

  <div class="stuffs">
    <div class="stuf">
      <i class="fas fa-headset"></i>players
    </div>

    <div class="stuf">
      <i class="fas fa-headset"></i>players
    </div>
  </div>
</div>

<div class="d-flex justify-content-around">
  <div class="post-form">
    <%= simple_form_for [@community, @post] do |f| %>
      <%= f.input :content %>
      <%= f.input :photo %>
      <%= f.input :video %>
      <%= f.submit %>
    <% end %>
  </div>
</div>




<%# <% if truevalue %>
  <%# <%= post.like += 1 %>
<%# <% end %>
